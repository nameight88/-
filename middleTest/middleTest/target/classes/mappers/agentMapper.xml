<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="agent">
	<!-- 
	<insert id="insertCustomer" parameterType="com.javassem.vo.CustomerVO">	
	INSERT INTO tx_customer(num, ssn, NAME, address) 
	VALUES(customer_tx_seq.nextval, #{ssn},#{name},#{address})
	</insert>
	 -->
	 
	<!-- mariadb -->
	<insert id="insertAgent" parameterType="com.javaclass.vo.AgentVO">	
	INSERT INTO agent(agent_id, agent_password, agent_name, agent_email, agent_phone, license_num, agency, agentImg_url ) 
	VALUES(#{agent_id},#{agent_password},#{agent_name},#{agent_email},#{agent_phone},#{license_num},#{agency},#{agentImg_url})
	</insert>
	
	<select id="ldCheck"  parameterType="com.javaclass.vo.AgentVO">
	SELECT agent_id FROM agent
	UNION
	SELECT user_id FROM user;
	</select>
	
</mapper>
